deferred class
	A[G->C1]

feature

	next: like Current

	a_feature(a_arg: INTEGER): STRING
		local
			a_local: like a_arg
		do
		
		end

	c: G

	test(a_var_a2: INTEGER; arg_c2_a2: C1)
		deferred
		end

end
