test2
	local
		list: LIST[X]
		linked_list: LINKED_LIST[C2]
		weird_list: LINKED_LIST[LIST[TUPLE[detachable ARRAY[like c_ren], X, TUPLE]]]
		str: STRING
		i: INTEGER
	do
		if attached {STRING}c_ren as l_c then
			if attached {STRING_8}l_c as l_etr_ot_local then
				str := l_etr_ot_local
			else
				str := Void
			end
			if attached l_c.count as cnt then
				if attached {INTEGER_32}cnt as l_etr_ot_local then
					i := l_etr_ot_local
				else
					i := Void
				end
			end
		end
		if attached {X}c_ren.c2_b as l_etr_ot_local then
			c_ren := l_etr_ot_local
		else
			c_ren := Void
		end
		if attached {LINKED_LIST[C2]}list as l_etr_ot_local then
			linked_list := l_etr_ot_local
		else
			linked_list := Void
		end
		if attached {LIST[C2]}linked_list as l_etr_ot_local then
			list := l_etr_ot_local
		else
			list := Void
		end
		if attached {LINKED_LIST[LIST[TUPLE[detachable ARRAY[C2], C2, TUPLE]]]}linked_list as l_etr_ot_local then
			weird_list := l_etr_ot_local
		else
			weird_list := Void
		end
	end