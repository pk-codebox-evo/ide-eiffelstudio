note
	description: "Class to write objects into Solr documents"
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	SOLR_OBJECTS_WRITER

inherit
	SEM_OBJECTS_WRITER

feature -- Basic operations

	write (a_queryable: like queryable)
			-- Write `a_queryable' into `medium'.
		do
		end

end
