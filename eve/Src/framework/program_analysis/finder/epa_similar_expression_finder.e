note
	description: "Find expressions that are similar to a given expression"
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	EPA_SIMILAR_EXPRESSION_FINDER

inherit
	REFACTORING_HELPER

feature -- Access

--	similar_expressions (a_expr: AFX_EXPRESSION; a_class: CLASS_C): DS_HASH_SET [AFX_EXPRESSION]
--			-- Expressions written in `a_class' that are similar to `a_expr'
--		do
--			
--		end

end
